# Core — required by scripts/reproduce_manuscript.py
pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14

# Text-dependent stages only (scripts/01 and 02); not needed to reproduce
# the manuscript's results from the deposited signals.
py3langid>=0.2
vaderSentiment>=3.3
transformers>=4.35
torch>=2.0
scikit-learn>=1.3
openpyxl>=3.1
